name: 'Auto Release Draft'
description: 'Drafts a GitHub release with the changes introduced'
author: 'Diego Nepomnaschy'
outputs:
  release-url:
    description: 'The URL of the GitHub release that was drafted'

runs:
  using: 'node12'
  main: 'dist/index.js'